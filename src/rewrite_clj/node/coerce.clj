(ns ^:no-doc rewrite-clj.node.coerce
  (:require [rewrite-clj.node.coercer]
            [rewrite-clj.potemkin.clojure :refer [import-vars]]))

;; TODO: what do I do here? if this is strictly internal, I do not need this clj compatability file
